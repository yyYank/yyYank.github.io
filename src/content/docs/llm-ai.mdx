---
title: "LLM(AI)メモ"
description: "LLM/AI関連のよく使うプロンプトメモ"
order: 11
---

## よく使うプロンプト


### ワンライナーでcommitしたいとき 

```
git add && git commmit形式のワンライナーをください
```

### コードレビューを依頼する

```
以下のコードをレビューしてください。
改善点があれば具体的に指摘し、修正案も提示してください。

コード：
```

### バグ調査を依頼する

```
以下のコードでエラーが発生しています。
原因を特定し、修正方法を教えてください。

エラーメッセージ:

コード:
```

### 仕様からコードを生成する

```
以下の仕様に基づいて[言語名]でコードを実装してください。

仕様:
- 
- 
```

### テストコードを書いてもらう

```
以下のコードに対するユニットテストを書いてください。
テストフレームワークは[フレームワーク名]を使用してください。

[コードをここに貼る]
```

### リファクタリングを依頼する

```
以下のコードをリファクタリングしてください。
可読性・保守性を重視してください。
動作は変えないでください。

[コードをここに貼る]
```

### ドキュメントを書いてもらう

```
以下のコードのドキュメントコメントを書いてください。
[言語/スタイル]の形式で記述してください。

コード：

```

### 技術的な概念を説明してもらう

```
[技術名・概念名]について、以下の観点で説明してください。
- 概要（初心者にもわかるように）
- 主なユースケース
- 具体的なコード例
```

### コードを別言語に翻訳する

```
以下の[元言語]コードを[対象言語]に書き直してください。
慣用的な書き方を優先してください。

[コードをここに貼る]
```

### 正規表現を作ってもらう

```
以下の条件に一致する正規表現を作成してください。
また、マッチする例・しない例もあわせて示してください。

条件:
[条件をここに書く]
```

### SQLクエリを書いてもらう

```
以下のテーブル定義に基づいて、[やりたいこと]を行うSQLクエリを書いてください。

テーブル定義:
[DDLをここに貼る]
```

### 処理の流れを図解してもらう

```
以下のコードの処理フローをMermaid記法で図示してください。

[コードをここに貼る]
```

### エラーメッセージを日本語で解説してもらう

```
以下のエラーメッセージの意味と、よくある原因・対処法を日本語で教えてください。

[エラーをここに貼る]
```
